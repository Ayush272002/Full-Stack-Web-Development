import React from 'react';
import ReactDOM from 'react-dom/client';
import ClassComponents from './ClassComponents';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<ClassComponents />);